# 21-ts-added-lint

## How to install modules

```bash
yarn install
rm -rf node_modules
yarn install --offline
```

## How to run

```bash
yarn lint
yarn test
```

## Creating jest config file (Optional)

```
npx jest --init
또는
npx ts-jest config:init
```
